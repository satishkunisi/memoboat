<form>
  <input type="text" 
         class="form-control twitter-typeahead"
         id="add-tag-input"
         placeholder="+ Add Tag"
         name="new_tag[name]"
         <% if (disabled) { %>
           disabled
         <% } %>
         >
</form>
