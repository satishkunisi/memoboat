<% if (memo) { %>
<%   var body_content = memo.get('body');  %>
<% } %>

  <textarea id="memo_body"
       name="memo[body]"
       placeholder="...to create a new memo."><%= body_content %></textarea>