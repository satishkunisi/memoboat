<% if (memo) { %>
<%   var body_content = memo.get('body');  %>
<% } %>

  <div id="memo_body"
       name="memo[body]"
       contenteditable="true"
       data-ph="...to create a new memo.">

       <%= body_content %>
  </div>
   <% if ((!memo.isNew()) && memo.hasImage()) { %>
     <img src="<%= memo.get('image_big_url')%>" class="img-thumbnail">
   <% } %> 